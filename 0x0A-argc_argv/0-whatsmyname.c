#include "main.h"


/**
 * main - check the code
 * @argc: the size
 * @argv: array of arguments
 *
 * Return: Always 0.
 */
int main(int argc, char *argv[])
{
	printf("%s\n", argv[argc * 0]);
	return (0);
}
